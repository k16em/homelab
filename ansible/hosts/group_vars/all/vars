---
alloy_homelab: "{{ vault_alloy_homelab }}"
llama_swap_models: "{{ vault_llama_swap_models }}"
local_llm_ip: "{{ vault_local_llm_ip }}"
maintainer_password_hash: "{{ vault_maintainer_password_hash }}"
nginx_password: "{{ vault_nginx_password }}"
opencode_default_model_name: "{{ vault_opencode_default_model }}"
opencode_small_model_name: "{{ vault_opencode_small_model }}"
root_password_hash: "{{ vault_root_password_hash }}"
tailscale_hosts: "{{ vault_tailscale_hosts }}"
