uid = sync
gid = sync
use chroot = no
max connections = 4
syslog facility = local5
pid file = /run/rsyncd.pid

[{{ ansible_architecture }}]
    path         = {{ pacman_cache_root }}/{{ ansible_architecture }}
    include      = **
    read only    = true
