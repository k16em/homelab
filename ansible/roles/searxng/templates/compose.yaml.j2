services:
  searxng:
    image: ghcr.io/searxng/searxng:latest
    volumes:
      - type: bind
        source: ~/.searxng
        target: /etc/searxng
    ports:
      - "80:8080"
      - "443:8080"
    restart: always

