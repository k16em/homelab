---
- name: 'containerdを再起動する'
  become: true
  ansible.builtin.systemd:
    name: containerd
    state: restarted
    daemon_reload: true

- name: 'kubeletが動いていれば再起動する'
  become: true
  ansible.builtin.command:
    cmd: systemctl try-restart kubelet.service
