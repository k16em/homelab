[Unit]
Description=Vibe Kanban
After=network.target

[Service]
Type=simple
User=agent
Group=agent
WorkingDirectory=/home/agent
Environment="PORT=28080"
Environment="HOST=0.0.0.0"
ExecStart=npx vibe-kanban
Restart=on-failure

[Install]
WantedBy=multi-user.target

